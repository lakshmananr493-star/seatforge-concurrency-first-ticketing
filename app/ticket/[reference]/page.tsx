import QRCode from 'qrcode'; import {db} from '@/lib/db'; import {notFound} from 'next/navigation';
export default async function Ticket({params}:{params:Promise<{reference:string}>}){const b=await db.booking.findUnique({where:{reference:(await params).reference},include:{event:{include:{venue:true}},items:{include:{showSeat:{include:{seat:true}}}},user:true}});if(!b)notFound();const qr=await QRCode.toDataURL(b.reference,{width:300,margin:2});return <main className="form card" style={{textAlign:'center'}}><div className="eyebrow">SeatForge ticket</div><h1>{b.event.title}</h1><p className="muted">{b.event.venue.name} · {new Date(b.event.startsAt).toLocaleString('en-IN')}</p><img src={qr} alt="Ticket QR" style={{width:260,maxWidth:'100%',background:'white',padding:12,borderRadius:12}}/><h2>{b.reference}</h2><p>Seats: {b.items.map(i=>i.showSeat.seat.rowLabel+i.showSeat.seat.seatNumber).join(', ')}</p><p className="muted">Present this QR at entry.</p></main>}
