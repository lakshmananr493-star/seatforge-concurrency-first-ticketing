import { db } from '@/lib/db';
export async function GET(req:Request){const u=new URL(req.url);const q=u.searchParams.get('q')||undefined;const city=u.searchParams.get('city')||undefined;const type=u.searchParams.get('type')||undefined;const events=await db.event.findMany({where:{published:true,startsAt:{gte:new Date()},...(q?{title:{contains:q,mode:'insensitive'}}:{}),...(city?{venue:{city:{equals:city,mode:'insensitive'}}}:{}),...(type?{type:type as any}: {})},include:{venue:true,_count:{select:{showSeats:true}}},orderBy:{startsAt:'asc'},take:50});return Response.json(events)}
