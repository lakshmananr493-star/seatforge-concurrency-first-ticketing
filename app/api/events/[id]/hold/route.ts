import { getSession } from '@/lib/auth'; import { holdSeats } from '@/lib/booking'; import { holdSchema } from '@/lib/validation'; import { jsonError } from '@/lib/http';
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return jsonError('Login required',401);try{const {id}=await params;const p=holdSchema.parse(await req.json());return Response.json(await holdSeats(id,s.id,p.seatIds),{status:201})}catch(e:any){return jsonError(e?.message==='SEATS_UNAVAILABLE'?'One or more seats are no longer available':e?.message||'Unable to hold seats',409)}}
