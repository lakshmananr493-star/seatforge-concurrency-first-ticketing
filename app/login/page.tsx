'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('customer@seatforge.dev'),[password,setPassword]=useState('Customer@12345'),[error,setError]=useState('');const r=useRouter();return <main className="form card"><h1>Welcome back</h1><p className="muted">Sign in to reserve a seat.</p><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn" onClick={async()=>{const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/dashboard');else setError((await x.json()).error)}}>Login</button>{error&&<p style={{color:'#fca5a5'}}>{error}</p>}<p className="muted">New here? <Link href="/register">Create account</Link></p></main>}
