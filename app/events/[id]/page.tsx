import { db } from '@/lib/db'; import { notFound } from 'next/navigation'; import SeatPicker from '@/components/SeatPicker';
export default async function EventPage({params}:{params:Promise<{id:string}>}){const e=await db.event.findUnique({where:{id:(await params).id},include:{venue:true}});if(!e)notFound();return <main><div className="hero" style={{paddingBottom:20}}><div className="eyebrow">{e.type} · {e.venue.city}</div><h1 style={{fontSize:48}}>{e.title}</h1><p>{e.description}</p><p className="muted">{new Date(e.startsAt).toLocaleString('en-IN',{dateStyle:'full',timeStyle:'short'})} · {e.venue.name}</p></div><SeatPicker eventId={e.id} /></main>}
