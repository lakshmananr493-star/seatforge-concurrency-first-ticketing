'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Register(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[role,setRole]=useState('CUSTOMER'),[error,setError]=useState('');const r=useRouter();return <main className="form card"><h1>Create account</h1><label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Role<select value={role} onChange={e=>setRole(e.target.value)}><option>CUSTOMER</option><option>ORGANISER</option></select></label><button className="btn" onClick={async()=>{const x=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,password,role})});if(x.ok)r.push('/dashboard');else setError((await x.json()).error)}}>Create account</button>{error&&<p style={{color:'#fca5a5'}}>{error}</p>}</main>}
